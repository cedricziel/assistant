import 'package:test/test.dart';
import 'package:assistant_api/assistant_api.dart';

// tests for VapidKeyResponse
void main() {
  final instance = VapidKeyResponseBuilder();
  // TODO add properties to the builder and call build()

  group(VapidKeyResponse, () {
    // Base64url-encoded uncompressed P-256 public key (65 bytes). Pass this as `applicationServerKey` to `PushManager.subscribe()`.
    // String publicKey
    test('to test the property `publicKey`', () async {
      // TODO
    });
  });
}
