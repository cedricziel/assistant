# Gymfile — default build settings for iOS.

scheme("Runner")
workspace("ios/Runner.xcworkspace")
export_method("app-store")
include_bitcode(false)
output_directory("build/ios/")
output_name("assistant.ipa")
