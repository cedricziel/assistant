## 1. Permanent-error classifier (TDD)

- [x] 1.1 Add failing unit tests in `crates/llm-provider/src/retry.rs` for the four scenarios in `bounded-llm-retry/spec.md` (Moonshot insufficient-balance, OpenAI insufficient_quota, real transient 429, real transient 503). Confirm RED.
- [x] 1.2 Add a private `is_permanent_billing_error(msg: &str) -> bool` helper covering the marker list in `design.md` (case-insensitive substring match).
- [x] 1.3 Modify `is_transient_error_message` to short-circuit to `false` when `is_permanent_billing_error` returns `true`. Confirm GREEN.
- [x] 1.4 Refactor: pull the marker list into a `const &[&str]` and the case-insensitive comparison into a single helper.

## 2. Shared cap constant

- [x] 2.1 Add `pub const MAX_TURN_REDELIVERIES: u32 = 10;` to `assistant-core` (`crates/core/src/bus.rs`) so `assistant-bus-nats` and `assistant-runtime` consume the same value.
- [x] 2.2 Re-export from `assistant_core` (`crates/core/src/lib.rs`) so call sites read `assistant_core::MAX_TURN_REDELIVERIES`.

## 3. NATS consumer max_deliver (TDD)

- [x] 3.1 Add an integration test in `crates/bus-nats` (`consumer_carries_max_deliver_cap`) that creates a consumer, fetches `consumer_info`, and asserts `max_deliver == MAX_TURN_REDELIVERIES`.
- [x] 3.2 Add `max_deliver: MAX_DELIVER` to the `pull::Config` in `consumer_for` (`crates/bus-nats/src/lib.rs`).
- [x] 3.3 Add a second integration test (`consumer_for_reconciles_legacy_unbounded_config`) that pre-creates a consumer with `max_deliver = -1`, invokes `consumer_for`, and asserts the consumer is reconciled to the new cap. `consumer_for` now detects mismatched `max_deliver` via `consumer.info()`, deletes the stale consumer, and recreates it with the current config — so deploys do not need an operator-run `nats consumer rm`.

## 4. MessageBus terminate operation (already implemented)

- [x] 4.1 `MessageBus::fail` already exists on the trait at `crates/core/src/bus.rs:336` — verified.
- [x] 4.2 NATS bus `fail` already calls `AckKind::Term` at `crates/bus-nats/src/lib.rs:419`.
- [x] 4.3 SQLite bus `fail` already exists at `crates/storage/src/message_bus.rs:184` (in-memory tests use this path).

## 5. Worker terminal branch (TDD)

- [x] 5.1 Added unit tests `transient_below_cap_nacks_with_backoff`, `transient_at_cap_terminates_with_cap_reason`, `permanent_terminates_immediately_regardless_of_count` in `worker.rs` covering the decision contract via the pure `classify_turn_failure` function. Used a pure-function abstraction in lieu of a full mock-bus harness — the worker's call site delegates the decision to this function, so the contract is fully testable without bus wiring.
- [x] 5.2 Replaced the `nack_delayed` call site (now around lines 420–470) with `match classify_turn_failure(transient, delivery_count) { NackForRetry(d) => nack_delayed; Terminate(reason) => publish + fail }`.
- [x] 5.3 Terminal `TurnResult` content is constructed inline: `format!("exceeded retry cap: {e}")` for cap exhaustion, `format!("Turn failed: {e}")` for permanent. The existing `bus.fail(msg.id)` call already terminates the message with `AckKind::Term`.
- [x] 5.4 Worker tests pass (5/5 GREEN).
- [x] 5.5 `permanent_billing_error_is_classified_permanent` test added — confirms a Moonshot-style 429+balance-suspended error is classified permanent so the worker terminates on first delivery.

## 6. Logging

- [x] 6.1 The terminal branch emits `error!(error, conversation_id, worker_id, delivery, "Transient turn error exceeded retry cap — terminating")` when `terminate_reason == ExceededRetryCap`. Verified by inspection — the existing `bus_consume_cx` span already records `error.message`.

## 7. End-to-end smoke

- [ ] 7.1 **Deferred.** A full e2e test driving a turn whose LLM call always returns the Moonshot insufficient-balance error requires a shared mock LLM provider in `crates/integration-tests`, which doesn't exist yet (the smoke harness uses real Ollama containers). The unit-test triangle — classifier (`retry::tests`), worker decision (`transient_failure_tests`), bus reconciliation (`consumer_for_reconciles_legacy_unbounded_config`) — covers the contract. Filing as a follow-up rather than blocking the credit-safety hotfix.

## 8. Lint / format / docs

- [x] 8.1 `cargo fmt --all` and `cargo clippy -p assistant-core -p assistant-llm-provider -p assistant-bus-nats -p assistant-runtime --lib -- -D warnings` are clean. Pre-existing clippy issues in `anthropic/mod.rs`, `moonshot/mod.rs`, `otel_spans.rs`, `scheduler.rs` are unrelated.
- [x] 8.2 `CHANGELOG.md` is auto-generated by release-please from conventional commits — the commit message itself populates the entry.
- [x] 8.3 No operator step needed: `consumer_for` auto-reconciles legacy `max_deliver = -1` consumers by delete-and-recreate. Documented in `design.md` Risks / Migration sections.

## 9. Deploy verification

- [ ] 9.1 After deploy to schorschvm, run `nats consumer info ASSISTANT_BUS <consumer-name>` for each pull consumer and confirm `max_deliver: 10`.
- [ ] 9.2 Confirm the still-stuck Morgenübersicht turn (conversation `44a4d7ce`) either terminates within 10 redeliveries or has already been purged.
