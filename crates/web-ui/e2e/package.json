{
  "name": "assistant-web-ui-e2e",
  "private": true,
  "description": "Playwright visual regression tests for the assistant web UI",
  "scripts": {
    "test": "npx playwright test",
    "test:update": "npx playwright test --update-snapshots",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
